import { useContext } from 'react';
import { UserContexts } from '~/layouts/component/Video/ListVideo';

function loadVideoEnd() {
    // const usecontex = useContext(UserContexts);
    // console.log(usecontex);

    return <div></div>;
}

export default loadVideoEnd;
